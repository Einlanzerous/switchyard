# @switchyard/mcp

Model Context Protocol server for switchyard — read and write tickets through a
hand-curated tool surface from any MCP-aware agent (Claude Desktop, Claude Code,
Cline, Gemini CLI).

📖 **Full documentation has moved to [`docs/mcp.md`](../docs/mcp.md)** — tool
surface, setup paths (fresh install vs. connecting to imperial-construct over
Tailscale), the token-access model, tests, and the smoke harness.
